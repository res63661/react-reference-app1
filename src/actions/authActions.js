import * as ReducerTypes from './types';
import axios from 'axios';

//Imp login, logout, new user. create action then reducer.  how does reducer get state?  who passes it in?  who invokes?

export function createUser(user)
{

}